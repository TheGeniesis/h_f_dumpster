# Gitlab CI/CD values search

Search for value in variables for a specific group (doesn't work in a recursive way)

## Setup

- Generate PAT with `API_READ` permisison in Gitlab
- Update .envrc with proper values
- Create a venv
- Run `pip3 install -r requirements.txt`
- Run `python3 main.go`
